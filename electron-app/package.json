{
  "name": "Lattice-Planner",
  "version": "1.0.0",
  "description": "Lattice-Planner 桌面客户端（Electron），支持任务 DDL 提醒与系统托盘",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . --overwrite --platform=win32 --arch=x64 --out=release/ --icon=icon.ico --prune=true --asar --ignore=node_modules/electron --ignore=node_modules/electron-builder --ignore=node_modules/electron-packager --ignore=package-lock.json",
    "dist": "electron-builder"
  },
  "keywords": ["Lattice-Planner", "electron", "desktop"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^38.2.2",
    "electron-builder": "^26.0.12",
    "electron-packager": "^15.5.2"
  },
  "dependencies": {
    "axios": "^1.12.2"
  }
}
