# 保研面试 PPT 内容参考

> 目标：10 分钟讲清「我做了什么 + 亮点在哪」。**Agent 是绝对主角**（占 70%），其余技术模块一句话带过。
>
> 贯穿全场的定位：**我不是在"用" Agent 框架，而是在"实现" Agent 运行时的关键机制。**
>
> 本文档每页给：标题 → 放什么 → 讲什么（话术） → 数据/证据。按你实际面试时长可删减标 ⚡ 的页。

---

## 页面结构总览（建议 12~14 页）

| 页 | 标题 | 作用 |
|---|---|---|
| 1 | 封面 | 项目名 + 一句话定位 |
| 2 | 项目是什么（产品视角） | 30 秒讲清"这是个什么东西" |
| 3 | 技术总览（一页全景） | 画架构图，让面试官一眼看到全貌 |
| 4 | 为什么自己写 Agent 内核 ⚡ | 立"懂机制"的人设 |
| 5 | 亮点一：MCP loopback 自己连自己 | 最优先讲的创新点 |
| 6 | 亮点二：Reflexion × CRAG 双闭环 | 失败处理的深度 |
| 7 | 亮点三：工具运行时可见性（分层遮蔽） | 最新、最前沿的贡献 |
| 8 | 亮点四：轮次收尾归因（TurnOutcome） | 与三呼应，体现体系化 |
| 9 | 亮点五：上下文工程（子代理 + 前缀缓存） | 技术深度 |
| 10 | 亮点六：Agent 评测体系（录制回放） | 证明"工程化"的关键 |
| 11 | 其他模块速览 ⚡ | RAG / MCP Server / 多模型路由一句话 |
| 12 | 踩坑与反思 | 展示成熟度，最有说服力 |
| 13 | 量化成果汇总 | 一页数据 |
| 14 | 未来方向 + 收尾 | 主动划边界 + 留钩子 |

---

# 第 1 页 · 封面

**放**：项目名 `Lattice-Planner`，一句话定位，你的名字/专业。

**定位语（一句话，务必背熟）**：

> 一个面向个人的目标管理应用，我给它内置了一个**从零手写的 Agent 运行时**——不依赖 LangChain 这类框架，自己实现了 ReAct 循环、工具系统、子代理、检索闭环和可观测层。

**副标题备选**：`把「读我的资料、帮我规划」从想法落地成一个可工程化的 Agent 系统`

---

# 第 2 页 · 项目是什么（产品视角）

**放**：左侧产品功能点，右侧一句"为什么会演进成 Agent"。

**讲什么**：

> 它本身是一个个人目标/任务/笔记管理工具。后来我想让它能"读我的资料、帮我规划"，就逐步演进成了一个内置 Agent 的系统。

**功能点（列 4~5 个即可）**：
- 任务/目标/笔记管理 + 多思维模式（执行/学习/规划）
- 规划得分（任务/目标/笔记三维 0~100 分）
- 内置 Agent：对话式读资料、做规划、复盘
- 桌面端（Electron）+ DDL 提醒

**关键一句（承上启下）**：

> 技术重点全在 Agent 这一块，其他是常规 CRUD，下面我主要讲 Agent 的运行时设计。

---

# 第 3 页 · 技术总览（一页全景图）

**放**：一张架构图（分层），每层一句话。

```
┌─ 编排层 ─────────────────────────────┐
│ AgentOrchestrator（ReAct 主循环）      │
│   + 子代理（规划/复盘/检索，独立上下文） │
│   + 工具可见性 / Reflexion / TurnOutcome│
├─ 工具层 ─────────────────────────────┤
│ ToolRegistry（@AgentTool 注解→反射）   │
│   本地工具 + MCP 远程工具              │
├─ 知识层 ─────────────────────────────┤
│ CRAG 纠正式检索 + RAG Serving          │
│ （hybrid 检索 / 语义缓存 / 预取）       │
├─ 接入层 ─────────────────────────────┤
│ MCP 双向（Server + Client）+ 多模型路由  │
└─ 可观测 ─────────────────────────────┘
│ 轨迹埋点 + 指标端点 + 评测回放体系      │
```

**讲什么（30 秒，指着图从上往下）**：

> 自下而上是：工具层用注解+反射把业务方法注册成工具；知识层做个人知识库的检索闭环；编排层跑 ReAct 循环并做子代理隔离；最下面是可观测和评测。整个系统**没用任何现成 Agent 框架**。

---

# 第 4 页 · 为什么自己写 Agent 内核 ⚡

**放**：三个"控制点" + 一句"我知道有框架"。

**讲什么**：

> 我先说明，我知道有 LangChain、Spring AI 这些框架。手写的原因是我要三个控制点，它们要么被框架封装掉、要么要 hack：
> 1. **步数上限**与耗尽后的降级措辞（给用户"请拆分任务"而非抛异常）
> 2. **工具按模式隔离**（学习模式不该看到写工具）
> 3. **确认策略能被用户运行时降权**（auto-approve 是用户级配置）

**收尾一句**：

> 这让我能真正理解 Agent 运行时的每一个机制，而不是"调 API 做了个聊天框"。

---

# 第 5 页 · 亮点一：MCP loopback —— 让 Agent 自己连自己

**放**：一张调用链图 + "为什么同进程绕一次 HTTP"的答案。

**讲什么（这是你的招牌，务必讲透）**：

> 最有意思的是 MCP loopback。Agent 读本地文档时，我**不走同进程直接调方法的捷径**，而是让它作为 MCP Client 连接本机自己的 MCP Server，完整走 SSE + JSON-RPC 握手。

```
LLM 决定调 mcp.loopback.local.read_document
  → ToolRegistry 识别 mcp. 前缀
  → McpToolProxy 连本机 /mcp/message
  → 同 JVM 的 MCP Server 处理
  → 结果经 SSE 推回
```

**必答追问（面试官几乎一定会问）**：

> 确实多一次本机回环，实测 5ms 以内，可忽略。但换来三件事：
> 1. **协议栈是真通的**——Server/Client 两套代码都被真实验证
> 2. **扩展零成本**——接第三方 MCP Server 只需加一个配置
> 3. **它暴露了真实 bug**——正因走真协议，我才发现相对 URI 的 bug，走捷径会潜伏到接外部服务器那天

**数据**：loopback 回环 < 5ms

---

# 第 6 页 · 亮点二：Reflexion × CRAG 双闭环

**放**：一张表（两类失败 → 两个机制）。

**讲什么**：

> Agent 有两类失败，大多数人只处理一类：

| 失败类型 | 现象 | 我的机制 |
|---|---|---|
| 工具**坏了** | 抛异常/参数错/工具不存在 | **Reflexion**：错误回灌自纠 |
| 工具**好了但没查到好东西** | 检索成功但质量差 | **CRAG**：改写重检索/降级明示 |

**重点讲第二类（更有区分度）**：

> 第二类最危险：检索返回一堆低相关片段，LLM 会拿着**编出看似有据的错误答案**——比明确说"我不知道"危险得多。
>
> 我的做法是 CRAG：检索完自评质量，低质就改写重检索，仍不行就在结果里塞一个**结构化降级信号**，强制模型明示"未找到强相关笔记，以下基于通用知识"。

**点睛**：把"提示词软约束"升级成"运行时硬闭环"——质量评估在服务端确定性完成，不赌模型自觉。

---

# 第 7 页 · 亮点三：工具运行时可见性（分层遮蔽）—— 最新贡献

**放**：四层 scope 链图 + 一句话点出修了什么 bug。

**讲什么（这是你最新、最前沿的贡献，讲出"参考了 DeepSeek Harness"的出处）**：

> 个人场景有个特点：同一个用户在不同模式下，工具的权限应该不同。我在"学习"模式不想让 Agent 改任务，在"执行"模式又需要。
>
> 原来的做法是给工具打 tag 按模式过滤，但我发现两个问题：
> 1. tag 过滤是 OR 语义，**只能"放行"不能"禁止"**——`note.create` 同时带 note 和 write 两个 tag，学习模式放行了 note，它照样能写笔记（这是个真实 bug）
> 2. 更关键的是，**过滤只影响给 LLM 的列表，不影响执行**——模型凭记忆照样能调通

**方案**：

> 我参考了 DeepSeek Harness 的分层工具遮蔽，做了四层 scope 链：全局 → 模式 → 角色 → 会话。运行时实时算出"当前能看到哪些工具"，支持显式 deny，并且每个工具都能给出"为什么不可见"的决策链。

**点睛（体现不是照搬）**：

> DSH 的分层遮蔽其实解决两件事：插件化实现替换、可见性收窄。我只需要后者——我的工具是注解扫描的，一个名字唯一实现，硬做"替换"就是过度设计。所以我刻意裁剪掉了它，只保留收窄 + deny。

**串起体系（金句）**：

> 这和我之前的 Reflexion 封禁组成**两层权限治理**：可见性决定"你本来能碰什么"，Reflexion 封禁决定"你这轮已经把什么碰坏了"。

---

# 第 8 页 · 亮点四：轮次收尾归因（TurnOutcome）

**放**：一个"粘性降级标记"的示意 + 修正了什么指标。

**讲什么（与第 7 页呼应，体现体系化思维）**：

> 我另一个发现是：系统里有大量的**静默信息丢失**。比如工具结果超过 4000 字符就硬截断，切掉了 LLM 不知道、用户更不知道。
>
> 我参考 DSH 的一个细节——它的 max-tokens 状态是**粘性**的，某一步被截断过，后面正常完成也不能把结束原因覆盖回"正常"。
>
> 我把这个思路落地成"粘性降级标记"：本轮只要发生过截断、CRAG 降级、或子代理结论被切，就置一个**只能置位不能清除**的标记。

**修正指标（体现严谨）**：

> 这顺带修正了一个被高估的指标——原来的收敛率把"中途丢过数据但最后蒙对了"也算成正常收敛。现在多了一个 `cleanConvergenceRate`，两者的差值就是被高估的部分。

**价值一句话**：

> 在还没能力"不丢信息"之前，先做到"不隐瞒信息丢失"。

---

# 第 9 页 · 亮点五：上下文工程（子代理 + 前缀缓存）

**放**：两个机制各一张小图。

**讲什么（两个都讲，但控制篇幅）**：

**① 子代理隔离**：

> 主对话上下文是稀缺资源。Agent 读了 50 页 PDF，原文全进历史，后面就被挤爆。我做了 Orchestrator-Worker：规划/复盘/检索三个角色在**独立上下文**跑完整 ReAct，只回压缩后的结论（≤4000 字符）。并做了并行 fan-out，超时的 worker 降级标注而不整体失败。

**② 前缀缓存 + 字节稳定化**：

> 每步 ReAct 都要重复构造 system prompt。我把前缀拆出来缓存复用。关键是**字节稳定化**——上游的 automatic prefix caching 按前缀字节完全一致才命中，我修了两个隐蔽问题：日期从精确时间改成天级、工具 schema 输出排序。然后用 usage 字段**闭环验证**缓存真的命中了。

**数据**：上游 prompt cache 命中 token 占比（跑一遍记录真实数字）

---

# 第 10 页 · 亮点六：Agent 评测体系（录制回放）

**放**：一个"录制/回放"示意图 + 指标列表。

**讲什么（这是把项目从"做过"升级到"做好"的关键页）**：

> 前面所有优化，怎么证明没让效果变差？LLM 是非确定的，没法写传统断言。
>
> 我的解法是把可替换点设在 **HTTP 边界**：录制真实响应，回放时固定"模型说了什么"，其余——ReAct 循环、工具反射、CRAG 分支、前缀缓存——全部真实执行。

**点睛（寻址策略，有区分度）**：

> 回放寻址我用混合策略：主键按调用序号（稳定），同时记录请求指纹做漂移检测（改了 prompt 测试仍能跑，但报告会告诉你"有 3 个用例录制已过期"）。

**指标**：收敛率 / 工具幻觉率 / 步数 P50-P95 / 平均 LLM 调用数。CI 里完全离线、零成本。

**收尾一句**：

> 这套体系是我做所有优化的"度量衡"——改任何东西，跑一遍就知道有没有退化。

---

# 第 11 页 · 其他模块速览 ⚡

**放**：四个点，每点一句话（不讲细节，被问再展开）。

- **RAG Serving**：个人场景重复提问率高，做**语义查询缓存**（阈值 0.93，写失效兜底）
- **MCP Server 双向**：对外把系统能力暴露成 MCP Server，Claude Desktop 等可直接接入
- **多模型路由**：按用户偏好路由 provider/model + 加权轮询
- **主动式 Agent**：晨报/晚报定时推送（复用现有事件链 + Electron 通知）

**讲什么**：一句带过即可——"这些是配套能力，重点是前面讲的 Agent 运行时设计。"

---

# 第 12 页 · 踩坑与反思（最有说服力的一页）

**放**：2~3 个真实 bug，每个一行现象 + 一句根因。

**讲什么（挑 2 个讲，编不出来才有分量）**：

**坑 1 · MCP loopback 永远连不上（相对 URI）**：
> 现象是 Agent 说"读不了本地文件"，但 curl 全对。根因是服务端返回的 endpoint 是**相对路径**，Java HttpClient 不接受相对 URI，握手一直失败，而日志写的是"将重试"——看起来会自愈其实不会。这坑让我学会用 curl 分治定位是 Server 还是 Client 的问题。

**坑 2 · CRAG 链路"看起来通了其实断了"**：
> 三个组件单测全对，但检索质量差时 Agent 还是编造。根因是降级信号只在零命中时返回，而"有命中但质量差"走不进那个分支。**每个组件单独都对，但组件间的信息传递断了**——这是单测的盲区，所以我后来专门加了端到端断言。

**收尾金句**：

> 这两个坑教会我两件事：一是做了优化必须能证明有效；二是可观测性必须有消费方，否则指标恒为 0 也不会被发现。

---

# 第 13 页 · 量化成果汇总

**放**：一张数字表（面试前跑一遍，填真实数字）。

| 指标 | 数值 | 从哪拿 |
|---|---|---|
| 收敛率 | % | `/api/agent/trace/stats` |
| 干净收敛率（新） | % | 同上（`turnEnd`） |
| 工具幻觉率 | % | 同上 |
| 自修复成功率 | % | 同上（`reflexion`） |
| 上游 prompt cache 命中占比 | % | `/api/agent/prefix-cache/stats` |
| 语义缓存命中耗时 vs 未命中 | ms | 可观测端点 |
| 单元测试通过数 | 53+ | gradlew test |

**讲什么**：

> 这些数字都能从我的可观测端点实时拿到。我面试前跑一遍记下真实值——我不说"感觉快了"，只说"缓存命中 8ms vs 未命中 105ms"。

---

# 第 14 页 · 未来方向 + 收尾

**放**：2~3 个方向 + 一句主动划边界。

**讲什么（主动划边界，比被问出来更成熟）**：

> 我清楚的知道当前边界：会话状态在内存（生产要用工作流引擎）；向量检索是万级全表 cosine（十万级必须换向量库）；评测还没做 LLM-as-Judge 的答案质量评分。

**未来方向（留钩子）**：

> 下一步我想做两件事：一是把工具的**执行层强制**补上（现在过滤了但没拦截）；二是把"主动式 Agent"从定时推送延伸到对话内——复盘完主动追问"明天有 3 个任务到期，要帮你排期吗"。

**收尾一句**：

> 这个项目最大的收获是：Agent 的难点不在"能不能调用工具"，而在**上下文、质量、可控性**这三件事——这也是我接下来读研想深入研究的方向。

---

# 附：三个核心话术金句（背下来，随时调用）

1. **定位**：我不是在"用" Agent 框架，而是在"实现" Agent 运行时的关键机制。

2. **双闭环**：工具坏了用 Reflexion 自纠，工具没查到好东西用 CRAG 降级——把提示词软约束升级成运行时硬闭环。

3. **两层治理**：可见性决定"你本来能碰什么"，Reflexion 封禁决定"你这轮已经把什么碰坏了"。

---

# 附：准备清单（面试前必做）

- [ ] 跑一遍 `./gradlew agentEval` 和可观测端点，记录真实数字填进第 13 页
- [ ] 默画第 3 页架构图（练到不看稿）
- [ ] 把第 5 页"为什么绕 HTTP"的答案背熟（几乎必问）
- [ ] 把第 12 页两个坑的排查路径想清楚（"最难 bug"必问）
- [ ] 准备第 14 页的边界回答（防被问穿）
