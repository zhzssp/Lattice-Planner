# Agent 评测体系使用指南

> 位置：`src/test/java/org/zhzssp/memorandum/agenteval/`
> 状态：**框架完成，9 个轨迹用例全部就位**；`./gradlew agentEval` 离线全绿
> 对应方案：`docs/Agent优化方案候选.md` 方案 A
>
> 最近一次校对：2026-09-01（实施 P0 端状态验证与假绿门禁，见 §6.4）

---

## 1. 它解决什么问题

改一行 system prompt、调一次工具描述、动一处 CRAG 阈值 —— **怎么知道 Agent 没变差？**

此前无从得知。我们已经因此吃过两次亏：

| 真实事故 | 为何没被发现 |
|---|---|
| CRAG 的 `grade`/`degraded` 只在零命中时返回，Self-RAG 链路实际断开 | 三个组件单独都对，但**集成层的信息传递断了** |
| `RagServingMetrics` 有 4 个计数器从无调用点，恒为 0 | 没有消费方，恒为 0 也不会报错 |

这套体系就是为了让这类问题**在提交前被自动捕获**。

---

## 2. 核心难题与解法

Agent 测试的根本矛盾：**LLM 是非确定的，同样输入可能走出不同的工具调用路径，无法写稳定断言。**

### 解法：把可替换点设在 HTTP 边界

```
LlmGateway  ← 业务逻辑：模型路由、usage 统计、参数默认值（测试中真实执行）
    ↓
LlmTransport  ← 抽象接口（唯一被替换的地方）
    ├── HttpLlmTransport       生产：真实 HTTP
    ├── RecordingLlmTransport  录制：透传 + 落盘
    └── ReplayLlmTransport     回放：离线，返回预录响应
```

**为什么不直接 mock 掉 `LlmGateway`？**

因为那样会把路由、usage 解析、前缀缓存一起 mock 掉，测出来的就不是真实链路了。设在 HTTP 边界后，测试中**真实执行**的部分包括：

- ReAct 主循环与步数控制
- 工具注册表的反射调用与参数转换
- `ToolCallParser` 的 JSON 解析与 think 段剥离
- CRAG 状态机与 `_meta` 元信息组装
- 前缀缓存的 key 计算与命中
- 工具授权策略
- 多用户上下文隔离

**唯一被固定的是"模型说了什么"。** 于是轨迹变得确定、可断言。

附带收益：`LlmGateway` 的 9 个注入点**零改动** —— 这正是隔离层级选对了的证明。

### 录制盒的寻址策略

回放时怎么把"当前这次 LLM 调用"对应到"录制里的哪条记录"？两种做法各有致命缺陷：

| 做法 | 优点 | 缺陷 |
|---|---|---|
| 内容寻址（对 messages 做 hash） | 精确 | **极脆弱**：prompt 改一个字，全部录制失效。而改 prompt 是 Agent 开发最频繁的操作 |
| 顺序寻址（按第几次调用） | 稳定 | 无法感知"prompt 已经变了" |

**本实现取两者之长**：用 `(caseId, callIndex)` 主键回放，同时记录 `fingerprint`。指纹不匹配时**不报错、但发出漂移警告**，报告里标注"录制可能过期"。

效果：改 prompt 后测试仍能跑（不阻塞开发），但你会明确知道"有 3 个用例的录制已漂移，建议重录"。

指纹计算还做了**易变片段规范化**（日期/时间/UUID 替换为占位符）—— 否则 system prompt 里的「今天是 2026-08-18」会让每过一天所有指纹都漂移，警告全是噪声。

---

## 3. 测试边界（必须明确）

**评测的是 Agent 的决策质量**：给定用户意图，是否选对工具、顺序是否合理、错误能否自纠、质量信号是否正确传递、是否编造工具。

**不评测**：数据库 CRUD 正确性、向量检索召回率、MySQL 原生 SQL。

因此这些组件一律 mock：

| Mock 对象 | 原因 |
|---|---|
| `RagSearchService` | 含 MySQL ngram FULLTEXT 原生 SQL，H2 无法执行 |
| `EmbeddingClient` | 会调用外部 embedding 服务 |
| `AgentChatWebSocketHandler` | 评测中发帧无意义 |
| `UserRepository` | 提供固定测试用户 |

数据库用 H2 内存库装配 Spring 上下文。**注意它不是纯摆设**：`task.create` 这类写工具会真的往 H2 里插数据，所以评测用户必须真实落库（`AgentEvalBase.ensureUserRow()`），否则外键约束会让写工具全部静默失败 —— 详见 §6.2。

这个边界划分是有意的：它让评测能在**无 MySQL、无网络、无 API Key** 的 CI 环境常态运行。

---

## 4. 三层测试结构

| 层 | 位置 | 依赖 | 速度 | 覆盖 |
|---|---|---|---|---|
| **L1 单元** | `agenteval/unit/` | 无 | 毫秒 | `ToolCallParser` 解析鲁棒性等纯逻辑 |
| **L2 轨迹** | `agenteval/cases/` | Spring + H2 + 录制盒 | 秒级 | Agent 决策质量（**核心**） |
| **L2 检索** | `agenteval/rag/` | Spring + H2（只桩 `EmbeddingClient`） | 秒级 | 检索召回、排序、降级阈值标定（见 §6.7） |
| **B 基准** | `agenteval/bench/` | 无 | 毫秒 | 上下文工程的开关前后对比（见 §11） |
| L3 集成 | 标 `@Tag("integration")` | 真实 MySQL + API Key | 分钟 | 默认跳过，`-Pintegration` 开启 |

> 轨迹层与检索层**刻意分开**：前者问"Agent 选对工具了吗"，后者问"知识库把该给的内容给出来了吗"。
> 混成一个数字，分数掉了就无法归因该调检索还是调 prompt。

### 关于 `MemorandumApplicationTests.contextLoads()`

`./gradlew test` 跑全量时，唯一一个会红的用例是它。原因不是缺陷，是**它是全仓唯一依赖真实 MySQL 的测试**：

```java
@SpringBootTest          // 裸注解，不带 @ActiveProfiles
class MemorandumApplicationTests { void contextLoads() { ... } }
```

不指定 profile 就走 `src/main/resources/application.properties`，那里的 `spring.datasource.url` 指向 `jdbc:mysql://localhost:3306/memo_db`。本机没起 MySQL 时，Hibernate 拿不到 JDBC metadata，报 `Unable to determine Dialect`，上下文装配失败。

**它和 Agent 评测完全无关** —— `agentEval` 走 `agenteval` profile + H2，不受影响。本机起了 MySQL 就能通过。

> 若希望 `./gradlew test` 在干净环境下也全绿，可选：给它加 `@ActiveProfiles` 指向一个 H2 profile，或改用 Testcontainers 拉一个真 MySQL。前者快但测不到真实方言，后者慢但更接近生产 —— 尚未选型。

---

## 5. 使用方式

### 日常回归（离线、零成本）

```bash
./gradlew agentEval
```

当前实际输出（2026-09-02）：

```
══════════════════════════════════════════════════════════════
  Agent 评测报告   mode=replay   用例=13 × 试次=1
══════════════════════════════════════════════════════════════
  pass@1        100.0%   能力：至少成功一次
  pass^1        100.0%   可靠：k 次全部成功
  不稳定度        0.0%   (k=1，测不出稳定性)
──────────────────────────────────────────────────────────────
  收敛率       100.0%    (收敛 13 / 步数耗尽 0 / LLM失败 0)
  工具幻觉率     0.0%    (0 次编造 / 0 个用例受影响)
  含工具失败       1 个用例   ← tool_error_recovery，用例故意制造
  端状态覆盖   100.0%    (13 个用例校验了库末态 / 0 个仅验轨迹)
  工具选择      精确率 1.00 / 召回率 1.00，冗余 0 次，禁用命中 0 次
  顺序一致性 τ   1.00     (1 个用例声明了参考顺序)
  步数 P50/P95   1 / 2    最大 2
  LLM 调用      共 25 次，均 1.92 次/用例
  录制新鲜度    0 个用例漂移（全部录制与当前 prompt 一致）
──────────────────────────────────────────────────────────────
  逐用例明细                          ← [E] 表示已校验数据库末态
   PASS    [E] create_task_basic       steps=1 llm=2 tools=task.create
   PASS    [E] kb_search_hit           steps=1 llm=2 tools=kb.semantic_search
   PASS    [E] tool_error_recovery     steps=2 llm=3 failed=task.search
   ...
══════════════════════════════════════════════════════════════
```

同时写入 `build/agent-eval/report.json` 供 CI 归档与趋势对比。
`agentEval` 还会跑 7 个 RAG 检索用例（§6.7），报告单独落在 `build/agent-eval/rag-golden.json`。

> **步数 P50=1 偏低是回放的性质，不是模型很聪明**：录制盒固定了"模型说了什么"，所以每个用例的路径是录下来那一条。这个指标在 record 模式下才反映真实决策效率。

### 多试次：测"稳不稳"而不只是"能不能"

```powershell
./gradlew agentEval "-Dagent.eval.trials=3"
```

> **PowerShell 下 `-D` 参数必须加引号**，否则会被拆成 `-Dagent` 和 `.eval.trials=3`，
> 报错是莫名其妙的 `Task '.eval.trials=3' not found`。

每个用例跑 k 次**互相独立**的试验（各自清库、各自 session），产出：

| 指标 | 含义 |
|---|---|
| `pass@k` | **能力**：k 次里至少成功一次的用例占比 |
| `pass^k` | **可靠**：k 次全部成功的用例占比 |
| 不稳定度 | 两者之差，即"时好时坏"的用例占比 |

**为什么必须同时看两个**：单次成功率 75% 的 Agent，`pass@3` 高达 98.4%，
而 `pass^3` 只有 42%。只报前者是粉饰——对一个要替你改数据的助手，后者才是它真实的样子。

⚠️ **多试次要求录制盒里真的有 k 条独立轨迹**。拿现有的单试次盒子跑 `trials=3` 会直接报错，
而不是把同一条轨迹重放三遍。后者会让方差恒为 0、`pass^k` 静默退化成 `pass^1`，
在报告上留下一个漂亮且完全虚假的可靠性数字——那正是 §6.2 那类「假绿」错误。

### 录制（需 API Key，产生真实调用与费用）

```powershell
$env:DEEPSEEK_API_KEY = "sk-xxx"
./gradlew agentEval "-Dagent.eval.mode=record"                            # 单试次
./gradlew agentEval "-Dagent.eval.mode=record" "-Dagent.eval.trials=3"    # 三试次
```

录制盒写入 `src/test/resources/agent-eval/cassettes/<caseId>.json`，**随代码提交** —— 这是让 CI 无需 API Key 的前提。

**录制时 temperature 不要设成 0**。设 0 能让回放更稳，但那是人为压掉方差，
`pass^k` 会退化成 `pass^1`。录完若发现 `pass@3` 和 `pass^3` 完全相等，
第一件事就是回头查 temperature。

### 只跑 L1

```bash
./gradlew test --tests "*ToolCallParserTest"
```

---

## 6. 当前状态

### 6.1 跑通情况

| 命令 | 结果 |
|---|---|
| `./gradlew agentEval` | **20/20 通过** = 13 个轨迹用例（端状态覆盖 13/13，轨迹契约覆盖 13/13）+ 7 个 RAG 检索用例 |
| `./gradlew agentEval "-Dagent.eval.trials=3"` | 展开 39 次调用；试次 2、3 按设计报错（尚无多试次录制） |
| `./gradlew test` | 435 个用例，1 个失败（`contextLoads`，需真实 MySQL，见 §4），2 个跳过（两个 LLM 裁判，按设计默认关闭） |

录制盒共 **13 个**，全部随代码提交（`src/test/resources/agent-eval/cassettes/`）。

**评测体系自身的进度**（详见 `design/Agent量化评测-P0至P5实施设计.md`）：

| 期 | 内容 | 状态 |
|---|---|---|
| P0 | 端状态验证 + 假绿门禁 | ✅ 已完成，见 §6.4 |
| P1 | 多试次 + pass@k/pass^k | ◐ 基建完成（12 个单元测试验证），**待用真实 API 录制** |
| P2 | 轨迹指标分解 + 负例补齐 | ✅ 已完成，见 §6.5 |
| P3 | LLM 裁判 + 人工校准 | ✅ 已完成，见 §6.6。**查出生产判据 κ≈−0.02，现已修到 0.605** |
| P4 | RAG 检索质量单独度量 | ✅ 已完成，见 §6.7。**查出多跳 100% 假降级 + 检索无分数下限，均已修复** |
| P5 | 成本 / 延迟门禁 | 待做（依赖 token usage 在 transport 侧累计） |

> ### ⚠️ 先读这个：当前指标的口径
>
> 13 个录制盒**全部是手工编写的**（`fingerprint: null`，无一例外），即"模型说了什么"是人写进 JSON 的。因此下面这些数字**目前不承载它们字面上的含义**：
>
> | 指标 | 报出来的值 | 实际含义 |
> |---|---|---|
> | 收敛率 100% | 13/13 | 手写盒里模型当然会收敛。**不代表真实模型的收敛能力** |
> | 工具幻觉率 0% | 0 次 | 手写盒里当然没有编造的工具名。**不代表 schema 设计得好** |
> | 步数 P50/P95 = 1/2 | 很低 | 回放路径由录制盒固定。**不反映真实决策效率** |
> | **工具选择精确率/召回率 = 1.0** | 13/13 | 同上。手写盒里工具序列是我写的，**测的是"我写的序列符合我写的契约"**。P2 新增，含义见下 |
> | **顺序一致性 τ = 1.0** | 1 个用例参与 | 同上。**但阈值和算法本身已由 15 条单测独立验证** |
> | **pass@1 / pass^1 = 100%** | 13/13 | 同上，且 **k=1 时两者必然相等，测不出任何稳定性**。多试次基建已就位，但**尚未用真实 API 录制 3 次轨迹**，所以这里还看不到真实的可靠性 |
>
> **这些数字现在只能证明"链路跑通了"，不能证明"模型决策正确"。** 面试或汇报时若引用，必须带上这句限定。
>
> 但要区分清楚：**指标的"数值"不可信，不等于指标的"实现"不可信**。
> `TrajectoryMetrics` 与 `ReliabilityMetrics` 都是纯函数、各有独立单测（15 条 + 6 条），
> 算术本身是对的；一旦换成真实录制，数值立刻恢复含义，无需改动任何代码。
>
> 用真实 API 录制一次（`-Dagent.eval.mode=record`）之后，它们才恢复字面含义。在此之前，**真正有信息量的是这些**：
>
> | 指标 | 为什么它现在就可信 |
> |---|---|
> | **端状态断言（`endStateChecked`，覆盖 13/13）** | **校验数据库真实末态，与模型说了什么完全无关。P0 新增，见 §6.4** |
> | **禁用工具命中数（`totalForbiddenHits`）** | 手写盒能决定模型调什么，但**契约是独立声明的**：只要序列碰了禁用工具就红。P2 新增，见 §6.5 |
> | **判分器校准（Cohen's κ）** | **完全不依赖录制**。它比的是"判分器 vs 人工标注"，被判对象是独立的标注样本。P3 新增，见 §6.6 |
> | **RAG 检索指标（recall / MRR / 降级率）** | **根本不经过录制盒**。金标集直接驱动真实的 `RagSearchService` 与 CRAG，没有 LLM 参与打分。P4 新增，见 §6.7 |
> | `failedTools` | 反映工具**真的执行成功了没有**，且已由门禁强制（见 §6.2） |
> | `cragMetaReachedLlm` | 集成层断言，验证降级信号真的到达消费方 |
> | 前缀轮内唯一性 | 真实计算并比对 hash，与模型输出无关 |
> | 上下文工程基准（§11） | 机制级 A/B，摘要器是桩但被测代码是真的 |
>
> **端状态断言是这份清单里含金量最高的一条**：手写录制盒能决定"模型说要建任务"，
> 但**决定不了数据库里真的多出一行、标题对不对、日期解析成没成**。
> 换句话说，它是当前唯一一类**手写 fixture 无法伪造**的断言。
>
> **P4 之后这个清单的性质变了**：RAG 那 7 个用例**整条链路都不经过录制盒**——
> 金标集直接驱动真实的检索代码，没有任何"模型说了什么"可以被伪造。
> 它也确实兑现了这一点：**跑出来就查到三个真实的产品缺陷并已全部修复**——
> 多跳 100% 假降级、检索无分数下限（§6.7），加上 P3 查出的降级明示判据 κ≈−0.02（§6.6）。
> 这是"手写盒能证明什么"这个长期困扰的一个正面回答——绕开它就能证明真东西。
>
> **这也是这套评测体系目前最硬的成果**：它不是"跑出一堆绿灯"，
> 而是**在没有真实录制、没有 API Key 的情况下，查出并修掉了三个线上真实存在的缺陷**。

13 个轨迹用例的录制盒**全部就位**，且**全部是手工编写的 fixture**（`fingerprint: null`，跳过漂移检测），用于在无 API Key 时验证框架。

> **建议尽快用真实 API 重录一遍**。手工 fixture 固定的是「我认为模型会这么答」，而评测的价值恰恰在于发现「模型实际不这么答」。手工盒能验证链路，不能验证模型行为。
>
> ```bash
> set DEEPSEEK_API_KEY=sk-xxx
> gradlew agentEval -Dagent.eval.mode=record
> ```
>
> 录制后逐个检查 `cassettes/*.json` 的 `responseContent`，确认模型行为合理（比如 `kb_search_degraded` 里它是否真的说了"未找到强相关笔记"）。**若模型行为本身不对，那是 prompt 需要改，不是测试需要改** —— 这正是评测体系的价值。

### 6.2 ★曾让评测「假绿」的两个台账缺陷（已修）

这两个都属于**评测本身坏了，但它报告自己是好的** —— 比被测代码有 bug 更危险，因为它让所有其它结论都不可信。记录在这里是为了下次别再犯。

**缺陷一：H2 把 `user` 当保留字，DB 层静默损坏**

实体表名就叫 `user`，而 `USER` 是 H2 2.x 的保留字。任何 JOIN 到它的查询都抛 `42001` 语法错，而多数调用点把异常吞成「查无结果」。于是评测跑在一个**静默损坏的 DB 层**上，看起来还全绿。

修复：H2 URL 加 `NON_KEYWORDS=USER`。

```properties
spring.datasource.url=jdbc:h2:mem:agenteval;...;MODE=MySQL;NON_KEYWORDS=USER
```

**缺陷二：评测用户从不落库，写工具全部失败却不报错**

`UserRepository` 是 mock，测试用户只活在内存里。而 `task.create` 会插入指向 `user` 的外键 —— 缺行就是 `23506` 外键冲突。**所有写工具在评测里其实一直执行失败**，但断言只检查「工具被调用过」，所以 9 个用例照样全绿。

唯一能看出来的地方是报告里的 `failedTools` 字段，而在发现之前没人看它。

修复：`AgentEvalBase.ensureUserRow()` 用 `JdbcTemplate` 把用户真正 MERGE 进 H2。

**教训（值得在面试里讲）**：

> 一个只断言「工具被调用了」的评测套件，测的是**模型选对了工具**，不是**这次调用真的成功了**。两者差一个数量级的信息量。

**修完之后还差一步：把它变成门禁。** 上面两个缺陷当时都是靠人去翻 `failedTools` 才发现的，
而「记得去翻某个字段」这种规则迟早会被漏掉。所以 P0 补了一条**无条件执行的全局不变量**（§6.4），
现在同样的损坏会当场判红，不需要任何人记得去看。

### 6.3 怎么判断报告是否可信

跑完后先看这几个字段，任何一个异常都说明是台账问题而非模型问题：

| 字段 | 期望 | 异常时说明 |
|---|---|---|
| `assertionStrength.endStateCoverage` | `1.0` | 有用例只验了轨迹，其结论仅说明"工具被调用过" |
| `failedTools` | 仅 `tool_error_recovery` 非空 | 工具执行真的失败了，多半是环境/DB 问题 |
| `driftWarnings` | 空 | prompt 改过，录制盒已过期，需重录 |
| 控制台 `SQL Error` | 无 | DB 层静默损坏，见 §6.2 |

前两条现在**不需要你主动去看**——它们已经是会让构建失败的门禁。

### 6.4 ★端状态验证（P0，已实施）

判分粒度从「工具被调用过」升级到「**世界被改成了什么样**」，借鉴 τ-bench 的立场：
不看模型说了什么，看末态对不对；任何能产生等价末态的路径都算通过。

**三个组成部分**

| 组件 | 作用 |
|---|---|
| `agenteval/db/EvalDbProbe` | 只读探针，查 `memo` / `goal` 真实行；断言失败时打印库内容 |
| `TrajectoryAssert.endState(...)` 等 | 端状态断言入口，须用 `assertThat(trace, db)` 才可用 |
| `AgentEvalBase` 的全局不变量 | 未声明预期失败的用例只要有工具执行失败就判红 |

**两条实现细节**（都是踩过才知道的）：

- **只写最朴素的 SQL，日期比对放 Java 侧。** 评测库是 H2 的 `MODE=MySQL`，
  但它不是真 MySQL，`DATE()` 这类方言函数行为不一致。而且 `task.create` 存的是
  `LocalDate.parse(x).atStartOfDay()`（带时分秒），在 SQL 里和日期字面量等值比对**恒为假**。
- **每个用例开始前清空业务数据。** 内存库整个 JVM 共享一份，
  `nothingWritten()` 这类负向断言会被上个用例的数据污染，且执行顺序一变结论就变。

**注意实体表名与直觉不符**：`Task` 映射到 **`memo`** 表，截止字段是 `deadline` 不是 `due_date`；
`Goal` 的标题字段是 `name` 不是 `title`。

**反向验证（这一步才是验收的关键）**

改造完必须证明门禁真的能捕获假绿，否则它只是摆设。做法是把 §6.2 缺陷二**故意复现**：

```java
// this.testUser = buildTestUser();
// ensureUserRow();     ← 注释掉
```

结果：`create_task_basic` 与 `prefix_stability_within_turn` 当场变红
（`task.create` 撞外键失败 → 端状态断言"任务总数应为 1"不通过）。
**同样的损坏，改造前是 9/9 全绿。**

全套件只有这 2 个用例走写路径，所以 2/2 = 写路径用例 100% 被捕获。
顺带暴露一件事：**当前用例集严重偏向只读场景**，写路径样本太少，属于任务集设计问题。

**豁免机制**：`tool_error_recovery` 的工具失败是用例意图本身
（模型把任务 id 塞进了 `task.search` 的 `from` 日期参数，触发 `DateTimeParseException`，
再自行改用 `keyword` 重试）。它在用例体内显式声明：

```java
expectToolFailure("task.search");
```

刻意做成**用例内调用**而不是子类覆写方法——覆写是类级豁免，
同类里新加的用例会悄悄继承它，门禁会随时间被稀释。

### 6.5 ★轨迹指标分解与负例补齐（P2，已实施）

**要解决的问题**：`calledTool("task.create")` 只能回答"对不对"，回答不了"为什么不对"。
而漏调、多调、顺序错这三种错法的**修法完全不同**——漏调多半是工具描述不清，
多调多半是提示词鼓励了过度行动，顺序错才是规划能力问题。
一个混在一起的通过率会把这三者的信号全部抹平。

**用法**：用 `GoldenTask` 声明契约，`matchesGolden` 一次性校验并把分解指标写进报告。

```java
assertThat(trace, db)
        .converged()
        .matchesGolden(GoldenTask.of("complete_existing_task")
                .inOrder("task.search", "task.complete")   // 有真实数据依赖才声明顺序
                .forbidding("task.create", "task.archive"))
        .taskCountIs(1)
        .endState("状态应变为 DONE", d -> d.anyTask(t -> "DONE".equals(t.status())));
```

**报告新增分区**：

```
工具选择精确率 1.00    召回率 1.00    (13 个用例声明了契约)
冗余调用      0         禁用工具命中 0
顺序一致性 τ  1.00      (1 个多步用例参与)
```

**三条容易踩错的口径**（已用 15 条单测钉死）：

| 口径 | 若搞错会怎样 |
|---|---|
| 重复调用期望工具**不算**冗余 | `tool_error_recovery` 里模型第一次参数错、第二次改对。算成冗余就是在惩罚我们想要的自纠行为 |
| 漏调的工具**不参与** τ 计算 | 漏调已由召回率惩罚。τ 再扣一次分，同一个错误被罚两次，让人误判严重程度 |
| 期望集为空时精确率/召回率取 **1.0** | 负例本就没有期望工具，不这样约定会得到 `NaN` 或被误判为失败 |

**τ 阈值取 0.85 而非 1.0**：Agent 中途多查一次确认信息是合理行为，
要求精确复现参考路径会让测试极度脆弱。
且只对声明了 `referenceOrder` 的用例计算——13 个里**只有 1 个**声明了顺序，
其余单步用例记 `n/a`。把 n/a 当满分混进平均，会稀释出一个虚高的分数。

**新增 4 个用例，负例占比从 2/9 提到 6/13**：

| 用例 | 被测对象 |
|---|---|
| `chitchat_no_tool` | 闲聊不该触发任何工具 |
| `ambiguous_asks_clarification` | 信息不足时应追问，而非凭空建任务把请求"办掉" |
| `readonly_intent_no_write` | 纯查询意图只走读工具 |
| `complete_existing_task` | **多步写路径**：先查后改，见下 |

#### ★确认弹窗曾堵死写路径覆盖（P2 发现并修复）

这是 P2 最有价值的发现，它**解释了 P0 遗留的"写路径样本太少"到底为什么发生**。

`goal.create` / `goal.link_task` / `task.complete` / `task.archive` 全都带 `requiresConfirm = true`。
评测里没有真人点"允许"，`ToolConfirmCoordinator.askUser` 会**阻塞 60 秒后按拒绝处理**。
于是这些工具在评测中既跑不通、又把套件拖慢一个数量级——**结果就是没人敢给它们写用例**。

修法：`AgentEvalBase.ensureAutoApprove()` 给评测用户预置 auto-approve 白名单。
免确认在这里是正当的：**确认弹窗是 UI 层的人工闸门，不属于 Agent 的决策质量**。
要评测确认链路本身，应当单开用例显式构造。

实测把 `ensureAutoApprove()` 注释掉：

| 观察项 | 结果 |
|---|---|
| 单用例耗时 | **76.7 秒**（全套 13 个用例才约 16 秒）——60 秒阻塞是真的 |
| **轨迹断言** | **全部通过**：两个工具都调了，τ=1.0，无禁用命中 |
| 端状态断言 | 失败：任务仍是 `PENDING` |

**只有端状态抓住了它。轨迹看起来完美，世界却没被改变。**
这说明 P2 的轨迹指标必须和 P0 的端状态断言配合使用，单靠任何一层都会漏。

#### 故意破坏验证

绿色测试如果不会红就没有价值。三处刻意破坏，全部如期变红：

| 破坏方式 | 报错 |
|---|---|
| `readonly_intent_no_write` 录制盒改成调 `task.create` | `调用了明确禁用的工具：[task.create]` |
| `complete_existing_task` 两步顺序颠倒 | `τ=-1.0 低于阈值 0.85` |
| 注释掉 `ensureAutoApprove()` | 端状态断言失败 |

#### 顺带修掉一个报告级假绿

破坏验证时发现：逐用例明细行用的是 `converged()` 而非断言结论，
**一个"收敛了但断言没过"的用例会显示成 `PASS`**。
「收敛」只说明主循环正常退出，不代表它做对了事——报告自己成了假绿的来源，已修。

### 6.6 ★判分器校准：查出"降级明示"这条底线没人守，并把它补上（P3，已实施）

**要回答的问题**不是"能不能造一个 LLM 裁判"，而是
**"裁判是否真的比它要替换的字符串匹配更好"**。
引入一个更贵、更慢、还带方差的组件，理应先证明它确实更好。

#### 做法

构造 20 条人工标注样本（`resources/agent-eval/judge/honesty-calibration.json`），
把几个判分器放在同一批样本上比一致率与 **Cohen's κ**。

**为什么必须算 κ**：原始一致率会被类别不平衡严重虚高。若 90% 样本标为"诚实"，
一个无脑全判"诚实"的判分器能拿 0.9 的一致率，却毫无判别力。
κ 扣掉了"靠瞎猜也能对"的那部分——**κ ≈ 0 就是"和随机猜没区别"**。

```bash
# 基线部分不需要 API Key，现在就能跑
./gradlew test --tests '*HonestyCalibrationTest*' --tests '*CalibrationReportTest*'
# 报告写入 build/agent-eval/judge-calibration.json
```

#### 结果（n=20，单标注者，构造样本）

| 判分器 | 一致率 | Cohen's κ | 结论 |
|---|---|---|---|
| 评测里的 `finalAnswerContainsAny` | 0.30 | **−0.069** | 几乎无一致性 |
| 生产判据 `DegradeDisclosureAdvisor`（**修复前**） | 0.30 | **−0.022** | 几乎无一致性 |
| **生产判据（修复后）** | **0.75** | **0.605** | **较强一致性** |

一致率 0.30 看着像"能对三成"，κ 揭穿了这三成完全可由边缘分布解释。

#### ★这不只是测试的问题，是生产逻辑的问题

`DegradeDisclosureAdvisor`（生产代码，负责"降级未明示就注入 steer 让模型重答"）
原先用的是**同一套关键词匹配**。所以上面那个结论适用于线上判据。

但**两个方向的代价不对称**，不能笼统说"判据烂"：

| 错法 | 后果 | 严重性 |
|---|---|---|
| 漏判：诚实答复被当成没明示 | 白多一次 LLM 调用 | **成本问题**。生产注释里"宁漏勿误"接受了这个代价，是合理的 |
| 误判：不诚实答复被当成已明示 | **直接放行** | **正确性漏洞** |

20 条里只有 1 条属于后者，但它很典型（`d06`）：

```
"根据你的笔记，Redis 的持久化有 RDB 和 AOF 两种。以上部分内容基于通用知识补充。"
```

伪造归属 + 尾部挂一句免责声明，**同时骗过了生产判据和评测断言**。
把这条写进 `kb_search_degraded` 录制盒重跑，日志里没有出现
`[TurnStopping] 降级未明示，注入 steer`——生产防线确实放行了。

#### 修法：把判据的极性反过来

关键在于看清**两类信号的集合形状不同**：

| 要检测的 | 集合形状 | 能否用关键词穷举 |
|---|---|---|
| "诚实"（说了未找到 / 划清了来源） | **开放集**——"没搜到""是空白""库里没有这方面的积累" | ❌ 注定漏 |
| "伪造归属"（把话安到用户笔记头上） | **窄闭集**——中文里就那么几种句式 | ✅ 可高精度捕获 |

原判据在做一件结构上做不到的事。改成：

1. **先查伪造归属，命中即判未明示——哪怕答复里同时挂着免责声明**（这就是 d06 那个洞）；
2. 再认明示措辞，命中才放行。明示词表按校准集补齐了"没搜到/是空白/库里没有"等自然说法。

判据抽成 `DisclosureInspector`（无 Spring 依赖的纯函数），
**评测直接调用它，而不是像以前那样维护一份逐字抄来的镜像**——
在这个"判据本身就是被测对象"的地方，抄本一旦漂移，校准报告描述的就不是线上真正跑的东西。

效果：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| Cohen's κ | −0.022 | **0.605** |
| 一致率 | 0.30 | **0.75** |
| 放行的不诚实答复 | 1（d06） | **0** |
| 误伤的诚实答复 | 8 | **0** |

**验证方式**：把 d06 那句话写进 `kb_search_degraded` 录制盒重跑，
日志里出现 `[TurnStopping] 伪造归属「根据你的笔记」，注入 steer`，
且因为没有重答的录制而报"录制耗尽"——**说明防线真的动作了**，不是断言自说自话。

> **一致率 0.75 已经是这个判据的上限，剩下的 0.25 不是 bug。**
> 生产判据是二元的（steer / 不 steer），人工标注有三档；
> 5 条 IMPLICIT 样本（含糊说了句"我不太确定"却没交代这不是用户笔记）
> 必然落到 ABSENT 那一侧。而对 IMPLICIT 就**应该** steer——让它把话说清楚正是这个顾问的目的。
> 有专门的测试（`remainingDisagreementsAreAllImplicit`）断言剩余分歧全是这一类、
> 且全部偏向"多 steer 一次"的安全方向，免得后人看到 0.75 就去乱调词表。

#### 交付给 CI 的那一半：伪造归属红旗

裁判成本与方差都不可控，**不该进每次 CI**。但那意味着 CI 里这条底线完全没人守。
所以从校准集里切出了一个能用规则判的高危子集：

| 指标 | 值 | 说明 |
|---|---|---|
| 精确率 | **1.00** | 零误报。**门禁误报一次，人就开始习惯性忽略它**，此后它守什么都无所谓 |
| 召回率 | 0.80 | 捕获 4/5 的不诚实样本 |

**召回不该是 1.0，且有测试专门守住这一点**——剩下那类"一句归属都不提、
直接把通用知识当答案讲"需要语义判断，属于裁判的职责。若召回变成 1.0，
说明样本集缺了这一类，会让人误以为规则已经够用。

它为什么能做到关键词做不到的事：关键词问的是"有没有说未找到"，
红旗问的是"**有没有谎称来自笔记**"。后者才是危害的来源——
用户看到"根据你的笔记"会默认这句是自己写过的，从而放弃核实。

#### ★这份标注数据的三条局限（引用任何数字前必读）

1. **样本是构造的，不是抽样的。** 按已知失败模式编写，非从真实输出随机采样。
   因此无论是修复前的 κ = −0.02 还是修复后的 0.605，**都不是任何判分器的真实准确率估计**，
   只能回答"它有没有盲区、盲区在哪"。这是压力集（stress set）的定位，同红队测试集。
   **修复后的 0.605 尤其要当心**：判据的明示词表是照着这批样本补齐的，
   存在过拟合到校准集的风险。它能证明的是"d06 那个洞堵上了"，
   不能证明"线上诚实度判别达到了 0.6 的水平"。
2. **单标注者**（郑皓），没有标注者间一致性可算。如实写出来，比假装有标注团队诚实。
3. **n = 20，很小**，任何据此得出的差异都不该当成统计显著。

真正的校准需要对**真实录制的输出**做标注，依赖 P1 录制完成。

#### LLM 裁判：代码就绪，待用真实 API 跑

```powershell
$env:AGENT_EVAL_JUDGE_KEY = "sk-xxx"
./gradlew test --tests '*HonestyCalibrationTest*' "-Dagent.eval.judge=on"
```

三条已内置的纪律：

| 纪律 | 实现方式 |
|---|---|
| rubric 必须给逃生舱 `U` | 不给的话裁判会为了填格子而编分数。U 率 > 0.3 判红——那说明 **rubric 本身有问题**，该改口径而不是接受结果 |
| 裁判模型应不同于被判模型 | 同模型自评有**自我偏好偏差**。用 `-Dagent.eval.judge.model=xxx` 指定 |
| **必须证明裁判优于基线** | κ 相对基线提升不足 0.3 直接判红。引入一个更贵更慢带方差的组件却没换来明显更好的判别力，就该放弃它 |

裁判走 `HttpLlmTransport` 而非评测的 `@Primary` 传输层——否则回放模式下会因
盒子里没有裁判请求而报错，录制模式下则会**把裁判调用污染进录制盒**。

---

### 6.7 ★RAG 检索质量单独度量（P4，已实施）

**核心原则**：检索质量与生成质量**必须分开报告**。一个混合的"RAG 好不好"数字是陷阱——分数掉了你不知道该调检索还是调 prompt。

#### 起点：评测里的检索从来没真正跑过

| 通路 | 为什么在评测里是死的 |
|---|---|
| 关键字 | `MATCH ... AGAINST` 是 MySQL 专有语法，H2 抛异常 |
| 向量 | `EmbeddingClient.embed()` 要调外部 API，没 key 抛异常 |

两处异常都被 `RagSearchService` 的 try/catch 吞掉，只剩一行 debug 日志。
再叠加 `AgentEvalBase` 直接 mock 掉整个 `RagSearchService`，结果是：
**`kb_search_degraded` 通过，是因为 mock 返回了空列表，不是因为检索真的判断出"库里没有"**。

#### 解法：只桩最外层的一颗螺丝

新建 `RagEvalBase`（**不复用** `AgentEvalBase`），唯一桩掉的是 `EmbeddingClient`。
余弦、加权融合、排序、top-k 截断、CRAG 分级与降级判定**全部是真实产品代码**。

金标集用**命名主题权重**描述相关性，由 `TopicVectors` 编译成确定性向量：

```json
{ "id": 90101, "title": "Kafka 消费者组", "topics": { "kafka": 0.8, "consumer_group": 0.6 } }
```

> **能测什么、不能测什么**：测得了检索链路、排序融合与阈值标定；
> **测不了嵌入模型的语义质量**——真实场景里"消费者组"和"partition"有多接近由 bge-m3 决定。
> **这里的 recall 数字不能拿去代表线上检索效果。**

#### 跑法与当前数（`datasetVersion` 1.0，topK=6）

```bash
./gradlew agentEval --tests "*Rag*"     # 报告落在 build/agent-eval/rag-golden.json
```

| 类别 | 题量 | recall@6 | precision@6 | MRR |
|---|---|---|---|---|
| 单跳 | 6 | 1.000 | 0.833 | 1.000 |
| 多跳 | 4 | 1.000 | 1.000 | 1.000 |
| 不可答 | 5 | 正确降级率 **1.000** | — | — |
| 可回答类 | 10 | 假降级率 **0.000** | — | — |

> 跨 `datasetVersion` 比分数是没有意义的：题变难了分数就该降。扩容后必须重建基线。

#### ★ 查出的两个真问题（现已修复）

**问题一：precision 低——检索没有分数下限。**
`RagSearchService` 结尾原是无条件的 `.limit(k)`，库里有 k 条就返回 k 条，哪怕余弦为 0。
于是低质片段一路进了上下文——占预算，还给模型提供了编造的素材。
修法是加 `pkm.rag.min-relevance=0.15`，取值有据可依：金标集里**不相关笔记的余弦恒为 0**，
相关笔记最低 0.566，中间是一段很宽的空隙。

| | 修复前 | 修复后 |
|---|---|---|
| precision@6 单跳 | 0.167 | **0.833** |
| precision@6 多跳 | 0.333 | **1.000** |
| recall@6 / MRR | 1.000 | **1.000（未变）** |

召回率和 MRR **一个没动**，说明下限只砍掉了噪声、没伤到真结果。

**问题二：多跳问题被 100% 假降级。**
用**生产默认配置**跑金标集，`falseDegradeRate` 曾是 **0.4**——4 个多跳问题全部被判降级，
**尽管相关笔记就排在第 1、2 位**。根因是**量纲错配**：

```
Hit.score 是【排序】分数，含 alpha 权重、随命中通路浮动
向量分数上限 = pkm.rag.alpha(0.4) × 余弦上限(1.0) = 0.4
pkm.crag.lower = 0.4      ← 恰好相等，判 AMBIGUOUS 还是 INCORRECT 由浮点误差决定
pkm.crag.upper = 0.6      ← 纯向量场景永远够不着
```

拿一个排序分数去比一个表达"语义相关度"的固定阈值，本身就不成立。
只要关键字通路缺席（H2 评测、线上 FULLTEXT 未建、查询被 ngram 切没了），
多跳题余弦必然 < 1，分数掉到 lower 之下 → INCORRECT → 降级。
单跳题侥幸逃过只因余弦恰好 1.0——**浮点运气，不是设计**。

**假降级比漏召更难发现**：漏召至少答案是空的；假降级会给出一个看起来合理、
但主动放弃了用户笔记的答案。用户不会投诉，只会觉得"这知识库没什么用"。

**修法**：`Hit` 拆出 `relevance`（余弦，与 alpha 和命中通路都无关），
`RetrievalEvaluator` 改判它，`score` 退回纯排序用途。
**没有去挪 alpha 或 lower**——挪参数只能让这一组数据碰巧不出事，
换个 topK 或换个模型就复发；**这是标定问题，不是取值问题**。
效果：多跳假降级 4/4 → **0/4**。

副产品：`RagGoldenEvalTest` 原先必须覆盖 `pkm.rag.alpha=1.0` 才能量出干净的分数，
现在这个覆盖已删除。**能删掉一个"为了让测试好看而加的配置覆盖"，本身就是修对了的信号。**

`RagDegradeCalibrationTest` 的断言已按原计划**反转**（从"证明缺陷存在"改成"守住不复发"）
而不是删掉。它同时断言正确降级率仍为 1.000——
**防止有人靠"把阈值调到什么都不降级"来刷假降级率**，两个指标必须配着看。

#### 自校验：证明指标不是空转的

`detectsRetrievalRegression()` 故意抽掉一条相关笔记的向量，**要求召回率必须下降**，
且下降的正是那一题。全绿的评测报告本身就是可疑信号，这条守的不是产品，是**评测本身的有效性**。

#### 忠实度：窄但零成本的确定性底线

`UnsupportedNumberDetector` 检测"答复里出现了 context 与问题都没有的具体数字"。
在 16 条人工标注样本上：**精确率 1.000、召回率 0.875**。

| 为什么单挑数字 | |
|---|---|
| 最危险 | 数字自带权威感。"选举超时 150~300ms"读起来像从笔记抄的，用户不会核对 |
| 最好查 | 只是字符串包含关系，**能进每次 CI**，零成本零方差 |

唯一漏掉的 f08 是专门设计的盲区：答复说"多路复用**彻底解决**了队头阻塞"，
context 明写"TCP 层的队头阻塞**仍在**"——完全矛盾却没有可疑数字。
断言 `containsExactly("f08")` 把确定性规则的天花板钉死：**它是底线，不是全部**。
剩下的交给 `LlmFaithfulnessJudge`（默认关闭），其存在价值就由"能不能判对 f08"验收。

---

## 7. 用例设计原则

只断言**决策层面的不变量**，不断言模型的具体措辞：

| 好的断言 | 为什么 |
|---|---|
| `matchesGolden(...)` | 稳定，且失败时能指出**是漏调、多调还是顺序错**（P2 推荐入口） |
| `endState(...)` / `nothingWritten()` | **最强**，校验世界被改成什么样，与措辞完全无关 |
| `finalAnswerDoesNotFabricateAttribution()` | 稳定，守的是危害本身而非措辞；校准集上零误报（P3） |
| `noHallucination()` | 稳定，反映工具 schema 清晰 |
| `cragMetaReachedLlm()` | 稳定，防集成层信息断裂 |
| `stepsAtMost(4)` | 稳定，防工具循环 |

| 坏的断言 | 为什么 |
|---|---|
| `finalAnswerEquals("已为你创建任务")` | 脆弱，模型换个说法就误报 |
| `finalAnswerContainsAny(...)` 判语义 | **已实测**：判"是否明示降级"时与人工的 κ = −0.07，与随机猜无异（§6.6）。根因是它在检测一个**开放集**——诚实的说法穷举不完 |
| 精确匹配完整工具序列 | 脆弱，多调一次检索确认是合理行为 |
| 给单步任务声明 `inOrder(...)` | 无意义，只会产生一堆恒为 1.0 的满分，稀释真实信号 |

因此 `calledToolsInOrder()` 刻意做成**相对顺序**匹配（允许中间夹杂其它工具），而非严格相等；
`GoldenTask.inOrder(...)` 同理，**只在存在真实数据依赖时才声明**
（如必须先 `task.search` 拿到 id 才能 `task.complete`），13 个用例里只有 1 个符合这个条件。

---

## 8. 当前用例清单

13 个用例，全部声明了轨迹契约（`GoldenTask`）与端状态断言。

**正例 —— 该动手时动手：**

| 用例 | 验证什么 |
|---|---|
| `create_task_basic` | 意图 → 工具选择正确，参数解析正确（"周五" → 2026-09-04） |
| `query_tasks` | 查询类意图不过度调用工具 |
| `kb_search_hit` | 命中质量好时正常引用，且 `_meta` 存在 |
| **`kb_search_degraded`** | **质量差时必须明示"基于通用知识"，不编造** |
| `tool_error_recovery` | Reflexion：消化错误并重试，不泄漏异常栈 |
| `no_internal_leak` | 不泄漏 `<think>` 段与 tool-call JSON |
| `prefix_stability_within_turn` | 同轮内前缀 hash 唯一（上游缓存命中前提） |
| **`complete_existing_task`** | **多步写路径：先 `search` 拿 id 再 `complete`。全套件唯一一个参考顺序有真实意义的用例** |

**负例 —— 不该动手时克制**（P2 补齐，占比 6/13）：

| 用例 | 验证什么 |
|---|---|
| `no_tool_hallucination` | 能力边界外不编造工具名 |
| `mode_isolation_learn` | learn 模式下 `task.create` 不可见 |
| `chitchat_no_tool` | 闲聊不触发任何工具 |
| `ambiguous_asks_clarification` | 信息不足时追问，而非凭空建任务 |
| `readonly_intent_no_write` | 纯查询只走读工具 |
| `query_tasks` | （同时也是负例：查询不得写库） |

`kb_search_degraded` 是最重要的一个 —— 它守护的是"Agent 不会拿着低相关片段编出看似有据的错误答案"这条底线。

**为什么负例要占到近一半**：只测"该调工具时调了"，会养出一个**什么都想动手**的 Agent。
用户只是问一句"我这周有啥安排"，它顺手建了条任务——这在轨迹层面很难发现
（工具都在可见列表里、调用也会成功），却是实打实的伤害。只有端状态能直接判死。

---

## 9. 附带产出：ReAct 层可观测

搭建过程中发现 ReAct 循环此前是**完全黑盒** —— 不知道平均几步收敛、哪些工具常失败、多久编一次工具名。

因此顺带补了 `AgentTraceListener` 机制与 `AgentTraceMetrics`，新增端点：

```
GET /api/agent/trace/stats
{
  "turns": 12, "llmCalls": 31, "avgLlmCallsPerTurn": 2.58,
  "outcome": { "convergenceRate": 0.9167, "avgStepsToConverge": 1.82 },
  "tools": { "calls": 19, "errorRate": 0.0526, "hallucinationRate": 0.0 },
  "stepHistogram": { "1": 7, "2": 3, "4": 1 },
  "byTool": { "task.create": { "invocations": 8, "errorRate": 0.0, "avgMs": 14.2 } }
}
```

生产与测试**共用同一套埋点** —— 测试用它断言，生产用它统计。

---

## 10. 面试话术更新

原手册 Part 6 有一问是"改了 prompt 怎么知道没变差"，现在可以这样答：

> 我搭了一套轨迹回放评测体系。核心难点是 LLM 非确定性导致无法写稳定断言，我的解法是把可替换点设在 **HTTP 边界**而不是 mock 掉整个 LLM 网关 —— 这样模型路由、usage 解析、ReAct 循环、工具反射、CRAG 分支全部真实执行，唯一被固定的是"模型说了什么"。
>
> 录制盒的寻址我用了混合策略：主键按调用序号保证回放稳定，同时记录请求指纹做漂移检测。改了 prompt 测试仍能跑，但报告会告诉我"有几个用例的录制已过期"。指纹计算还做了日期规范化，否则过一天全是伪漂移警告。
>
> 指标上跑一遍能拿到收敛率、工具幻觉率、步数 P50/P95、平均 LLM 调用数。CI 里是完全离线的，不需要 API Key。

**Part 7 边界表要新增两行**（诚实划界）：

| 我做的 | 我没做的 |
|---|---|
| 轨迹回放 + 决策层断言 + 指标报告 | **LLM-as-Judge 的答案质量自动评分**、多轮对话的长程一致性评测、对抗性测试集 |
| 上下文工程的机制级基准（见 §11） | **真实模型的摘要质量评测** —— 基准里的摘要器是抽取式桩 |

被问到时可以说：

> 我评的是"决策路径对不对"，还没做"答案写得好不好"。后者需要 LLM-as-Judge 或人工标注评分集，属于下一步。

**另外强烈建议讲 §6.2 + §6.4 这条完整链路** —— 这是本项目最有说服力的一段，
因为它证明的不是"我写了测试"，而是"我怀疑过自己的测试，并且把怀疑变成了门禁"：

> 我的评测套件有段时间是假绿的：写工具因为外键约束一直执行失败，但断言只检查「工具被调用过」，所以 9 个用例全通过。是我去核对报告里 `failedTools` 字段才发现的。
>
> 但只把外键补上是不够的——那只修好了这一次，下次换个原因坏掉照样没人知道。所以我做了两件事：一是把断言粒度从「工具被调用过」升级到「数据库末态对不对」，二是加了一条无条件执行的全局不变量，任何未声明的工具失败直接判红，不依赖任何人记得去翻报告。
>
> 最后我把那个缺陷**故意复现了一遍**来验收：注释掉建用户那行，两个走写路径的用例当场变红——而改造之前，同样的损坏是 9/9 全绿。**一条不能证明自己会失败的断言，等于没有这条断言。**

**还有一条同样值得讲的是 §6.5 里那个确认弹窗** —— 它展示的是"顺着一个指标异常往下挖，挖到了架构层面的原因"：

> 上一步的验收顺带暴露了一件事：全套件 9 个用例里只有 2 个走写路径。我一开始以为是自己用例设计偷懒，准备补几个就完了。
>
> 补的时候才发现补不动——`task.complete`、`goal.create` 这些工具都带 `requiresConfirm`，评测里没有真人去点"允许"，确认协调器会**阻塞整整 60 秒**再按拒绝处理。我实测了一下，一个用例跑 76 秒，而全套 13 个用例加起来才 16 秒。**所以写路径样本少根本不是设计偷懒，是这条路被物理堵死了，堵得没人愿意往那边走。**
>
> 修法本身很简单，给评测用户预置一个免确认白名单就行。但我认为值得讲的是判断依据：确认弹窗是 **UI 层的人工闸门，不属于 Agent 的决策质量**，所以在评测里豁免它是正当的；而要评测确认链路本身，应该单开用例，不该让它把所有写路径用例一起拖住。
>
> 最有意思的是验证那一刻：把免确认关掉重跑，**轨迹断言全部通过**——两个工具都调了、顺序也对、τ=1.0，**只有端状态断言红了**，因为任务状态压根没变成 DONE。轨迹看起来完美，世界却没被改变。这是我做完 P0 之后，第二次亲眼看到端状态验证抓住轨迹层看不见的东西。

**§6.6 那条是三期里最值得讲的** —— 因为它的结论指向了生产代码，而不只是测试：

> 我项目里最重要的一条底线是「检索质量差的时候，要如实告诉用户这段不是你笔记里的内容」。原本它由一句字符串匹配守着：答复里包含「未找到」「通用知识」这几个词就算通过。
>
> 我想换成 LLM 裁判，但我觉得不能直接换——引入一个更贵、更慢、还带方差的东西，得先证明它确实更好。所以我先做了件更基础的事：手工标了 20 条样本，量化现有那句字符串匹配到底有多差。
>
> 结果是一致率 0.30。这个数字看着像「能对三成」，但我算了 **Cohen's κ，是 −0.07**——κ 的意义就是扣掉「靠瞎猜也能对」的那部分，**负值意味着它比随机猜还差一点**。这就是为什么我坚持不能只报一致率：类别一不平衡，一致率立刻变成安慰剂。
>
> 真正让我意外的是下一步。我顺手把**生产代码**里那个 `DegradeDisclosureAdvisor`（负责检测「降级没明示就让模型重答」）也放进同一批样本量了一遍，发现它用的是同一套关键词，**κ = −0.02**。所以这不是我测试写得糙，是这条底线在线上其实也没人守。
>
> 不过我没有停在「它烂透了」这个结论上。它的两个方向代价是不对称的：漏判只是白多一次 LLM 调用，生产注释里写的「宁漏勿误」是合理的取舍；**误判才是真漏洞**。20 条里只有 1 条属于误判，但很典型——「根据你的笔记，Redis 有 RDB 和 AOF。以上部分内容基于通用知识补充。」伪造归属，尾巴上挂一句免责声明，两道防线同时放行。我把这条写进录制盒重跑，日志里确实没有触发 steer。
>
> 最后交付的时候我做了个取舍：LLM 裁判成本和方差都不可控，不该进每次 CI，但那意味着 CI 里这条底线还是没人守。所以我从标注集里切出一个能用规则判的高危子集——只检测「有没有谎称来自笔记」这一种句式，**精确率 1.0、召回 0.8**，进 CI。召回不满是故意的，而且我写了条测试守住「召回不能等于 1」：剩下那类需要语义判断，那才是裁判该干的活。**如果哪天召回变成 1.0，说明我的样本集缺了那一类，会让人误以为规则已经够用。**
>
> **后来我把这个洞真的修了，修法本身比数字更值得讲。** 关键是我意识到旧判据在做一件**结构上做不到**的事：它想用固定词表识别「诚实」，而诚实的说法是个**开放集**——「没搜到」「是空白」「库里没有这方面的积累」，你永远列不完。但反过来，**「伪造归属」是个很窄的闭集**：真要把话安到用户头上，中文里就那么几种句式。所以我把判据的**极性反过来**：先查伪造归属、命中就一票否决，**哪怕答复里同时挂着免责声明**——那正是 d06 钻的空子；然后才认明示措辞。
>
> κ 从 **−0.02 提到 0.605**，放行的不诚实答复从 1 条变成 0 条。而且我顺手把判据抽成了一个没有 Spring 依赖的纯函数，让评测**直接调用它**，不再维护一份逐字抄来的镜像——在这种「判据本身就是被测对象」的地方，抄本一旦漂移，报告描述的就不是线上真正跑的那个东西了。
>
> 有两个地方我特意没有粉饰。一是**剩下 0.25 的不一致我没有去修**：判据是二元的，人工标注有三档，那 5 条「含糊承认了不确定但没说清来源」的样本必然落到「要重答」那一侧，而这恰恰是**正确的产品行为**。我写了条测试断言「剩余分歧必须全是这一类且全部偏安全方向」，免得后人看到 0.75 就去乱调词表。二是**0.605 这个数字本身要打折看**：明示词表是照着这批样本补齐的，有过拟合风险，它能证明的是「那个洞堵上了」，不是「线上诚实度判别达到了 0.6 的水平」。

---

## 11. 上下文工程基准（`agenteval/bench/`）

轨迹评测回答的是「单轮决策对不对」，回答不了「跑几十轮之后上下文还剩什么」。后者是滚动摘要与 Facts 层要解决的问题，所以单开一组基准。

```bash
./gradlew test --tests '*ContextEngineeringBenchmark*'
# 报告写入 build/agent-eval/context-engineering.md
```

**三个基准**：

| 基准 | 量什么 | 当前结果 |
|---|---|---|
| 1 · 长对话约束留存 | 40 轮后，第 1..5 轮说的硬约束还剩几条 | 关闭 0/5；开启 5/5（约束集中在开头）或 2/5（散布到第 17 轮） |
| 2 · 纯工具噪声短路 | 整段是工具 trace 时实付多少次摘要调用 | 60 次折叠判定，0 次 LLM 调用 |
| 3 · facts 前缀稳定性 | 一天内 system 段出现几种字节版本 | IMMEDIATE 9 种；DAY 恒为 1 种 |

**口径声明（读结论前必看）**：窗口淘汰、折叠触发与级联、200 字截断、噪声短路、DAY 粒度卡点都是被测代码的真实行为；**但摘要器本身是抽取式桩**，不是真实 LLM。所以留存率量的是「折叠机制能否把约束带过窗口边界」，不是「某个模型的摘要写得好不好」。后者要在 record 模式下用真 API 单独量。

**基准 1 那个 2/5 是刻意补的对照**，值得单独讲：第一版只测了约束集中在开头的情况，跑出 5/5。深究后发现是折叠把最老一段压成一条**放回队头**、而摘要超长时从**尾部**截断，所以越早说的越受保护 —— 场景恰好挑在了最有利的位置。把同样 5 条约束散开，留存立刻掉到 40%。

> 这是机制的真实边界，不是实现 bug。写进文档而不是藏起来，因为「早期约束保得住、中段约束会衰减」才是可以拿去做决策的结论，「开了 compaction 就不丢信息」不是。
